#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
PYTHON_BIN="$SCRIPT_DIR/venv/bin/python3"

if [ -x "$PYTHON_BIN" ]; then
  exec "$PYTHON_BIN" "$SCRIPT_DIR/omnimem.py" "$@"
fi

exec python3 "$SCRIPT_DIR/omnimem.py" "$@"
