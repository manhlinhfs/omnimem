"""OmniMem benchmark suite.

Benchmarks are reproducible scripts that measure retrieval accuracy, latency,
and parser quality. Each writes a JSON result file under `benchmarks/results/`
so numbers can be compared across releases.
"""
